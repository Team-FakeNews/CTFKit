# CTFKit

This branch should be using :

* sphinx
* pylint
* virtualenv
* unittest

## Documentation

* RST format
* In the docs/ directory

### Run CTFKit
* `pipenv shell`
* `pipenv install`
* `pip install -e .`
* `ctfkit`
